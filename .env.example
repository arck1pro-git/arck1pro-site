# Chave da API da Airticles (server only, sem prefixo NEXT_PUBLIC_).
# Em produção, cadastre em Vercel > Settings > Environment Variables.
# Nunca versione a chave real: .env* está no .gitignore.
BLOG_API=
